# Agent Definition — generated by Squad Protocol Engine v4.0.0
# Source of truth: SQUAD_PROTOCOL.md Section 5.1

agent:
  name: "{{AGENT_NAME}}"
  id: "{{agent-id}}"
  title: "{{AGENT_TITLE}}"
  icon: "{{EMOJI}}"
  whenToUse: "{{WHEN_TO_USE}}"
  customization: null

persona:
  role: "{{ROLE_DESCRIPTION}}"
  style: "{{COMMUNICATION_STYLE}}"
  identity: "{{IDENTITY_STATEMENT}}"
  focus: "{{PRIMARY_FOCUS}}"
  core_principles:
    - "{{PRINCIPLE_1}}"
    - "{{PRINCIPLE_2}}"
    - "{{PRINCIPLE_3}}"
  responsibility_boundaries:
    owns:
      - "{{OWNS_1}}"
    delegates:
      - "{{DELEGATES_1}}"
    escalates:
      - "{{ESCALATES_1}}"

persona_profile:
  archetype: "{{Builder|Guardian|Analyst|Creator}}"
  communication:
    tone: "{{technical|analytical|strategic|creative}}"
    emoji_frequency: low
    vocabulary: professional
    greeting_levels:
      minimal: "{{ICON}} {{agent-id}} ready."
      named: "{{ICON}} {{AGENT_NAME}} ({{ARCHETYPE}}) ready."

commands:
  - name: "*{{command-1}}"
    visibility: public
    description: "{{COMMAND_1_DESCRIPTION}}"
    args: []
  - name: "*{{command-2}}"
    visibility: public
    description: "{{COMMAND_2_DESCRIPTION}}"
    args:
      - name: "{{arg-name}}"
        required: true
        description: "{{ARG_DESCRIPTION}}"

dependencies:
  tasks:
    - "{{task-name}}.md"
  templates: []
  checklists: []
  data: []
  tools: []
  git_restrictions:
    allowed_operations: ["git add", "git commit", "git status", "git diff", "git log"]
    blocked_operations: ["git push", "git force-push"]
    redirect_message: "Delegate push operations to @devops"

autoClaude:
  specPipeline: false
  execution: true
  recovery: true
  qa: false
  memory: true
  worktree: false
